using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class minichunkb : MonoBehaviour {
	/*public bool tickon = true;
	public int ticknum = 1;

	public void dotick(int i){
		if(i == ticknum)StartCoroutine("dotick2");
	}
	
	public IEnumerator dotick2(){
		yield return new WaitForEndOfFrame();
		foreach(Transform child in transform){
			if(child.GetComponent<block>().tickon)child.GetComponent<block>().dotick(1);
		}
	}
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}*/
}
