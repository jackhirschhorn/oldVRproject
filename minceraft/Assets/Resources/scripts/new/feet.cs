using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class feet : MonoBehaviour {
	/*public LayerMask mask1;
	private RaycastHit hit;
	public Transform cam;
	// Update is called once per frame
	void Update () {
		Ray ray = new Ray(cam.position, Vector3.down);
		if(Physics.Raycast(ray, out hit, 10, mask1, QueryTriggerInteraction.Ignore)){
			transform.position = hit.point;
		}
		//Debug.Log(cam.localRotation.y);
		if(ViveControllerVR.moving || (cam.eulerAngles.y+75 < 359 ? transform.eulerAngles.y >= cam.eulerAngles.y+75 : transform.eulerAngles.y-75 >= cam.eulerAngles.y) || (cam.eulerAngles.y-75 > 0 ? transform.eulerAngles.y <= cam.eulerAngles.y-75 : transform.eulerAngles.y+75 <= cam.eulerAngles.y)) transform.localRotation = Quaternion.Euler(0,cam.eulerAngles.y,0);
	}*/
}
